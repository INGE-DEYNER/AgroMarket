name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate CHANGELOG with git-cliff
        id: gitcliff
        uses: orhun/git-cliff-action@v2
        with:
          config: ./cliff.toml
          output_file: CHANGELOG.md

      - name: Upload CHANGELOG artifact
        uses: actions/upload-artifact@v3
        with:
          name: CHANGELOG
          path: CHANGELOG.md

      - name: Read CHANGELOG into output
        id: read_changelog
        run: |
          echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
          cat CHANGELOG.md >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          body: |
            ${{ steps.changelog.outputs.CHANGELOG }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image (versioned)
        run: |
          cd agroMarket
          IMAGE=ghcr.io/${{ github.repository_owner }}/agromarket:${{ github.ref_name }}
          docker build -t $IMAGE .
          docker tag $IMAGE ghcr.io/${{ github.repository_owner }}/agromarket:latest
          docker push $IMAGE
          docker push ghcr.io/${{ github.repository_owner }}/agromarket:latest

      - name: Deploy to Kubernetes (update image)
        if: ${{ secrets.KUBECONFIG != '' }}
        env:
          KUBECONFIG_DATA: ${{ secrets.KUBECONFIG }}
        run: |
          echo "$KUBECONFIG_DATA" | base64 --decode > $HOME/.kube/config
          kubectl -n default set image deployment/agromarket agromarket=ghcr.io/${{ github.repository_owner }}/agromarket:${{ github.ref_name }} --record || true
