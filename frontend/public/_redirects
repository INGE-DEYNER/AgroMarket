# Cloudflare Pages - SPA Redirects para AgroMarket
# Todas las rutas que no sean archivos reales se redirigen a index.html
# para que React Router maneje el enrutamiento del lado del cliente

# Redirigir todas las rutas al index.html (SPA)
/*    /index.html   200

# APIs y archivos estáticos - NO redirigir (dejar que fallen con 404 si no existen)
# Las llamadas al backend se hacen directamente a api.agro-market.app

