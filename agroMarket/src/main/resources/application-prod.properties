# === PERFIL PRODUCCIÓN — AgroMarket ===

# Puerto
server.port=${SERVER_PORT:8080}

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# OAuth2 Google
# OAuth2 Google
spring.security.oauth2.client.registration.google.client-id=979388358435-5meqv2lerljaipdqcg6eot18h9m5ht3p.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.provider.google.issuer-uri=https://accounts.google.com

# JWT
app.jwt.secret=${JWT_SECRET:AgroMarket2026SuperSecretKeyParaJWTProduccionMinLength256bits!!}
app.jwt.expiration=86400000
app.security.id-encryption-secret=${ID_ENCRYPTION_KEY:ClaveEncriptacion32CharsDefaultKey!}

# Frontend URL
app.frontend-url=${APP_FRONTEND_URL:https://agro-market.app}

# CORS
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://agro-market.app}

# Brevo email
brevo.api.key=${BREVO_API_KEY:mock-key}
brevo.sender.email=${BREVO_SENDER_EMAIL:ad24ff001@smtp-brevo.com}
brevo.sender.name=${BREVO_SENDER_NAME:AgroMarket}

# Logging
logging.level.root=WARN
logging.level.com.agromarket=INFO

# Deshabilitar lazy initialization en producción para asegurar la creación del bean cacheManager
spring.main.lazy-initialization=false

# Deshabilitar Flyway en producción y excluir la autoconfiguración
spring.flyway.enabled=false
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration
